class Hackathon {
    // ──────────────────────────────────────────────────────────
    // 1. Название мероприятия
    //    Как назвать? - eventName
    //    Секретно? - нет
    //    Изменяется? - нет - val
    //    При инициализации? - да
    //    Тяжёлая инициализация? - нет
    //    Валидация? - нет
    //    Геттер? - нет
    //    Private set? - нет (val)
    // ──────────────────────────────────────────────────────────
    val eventName: String = "Hackathon 2026"

    // ──────────────────────────────────────────────────────────
    // 2. Дата проведения
    //    Как назвать? - eventDate
    //    Секретно? - нет
    //    Изменяется? - нет - val
    //    При инициализации? - да
    //    Тяжёлая инициализация? - нет
    //    Валидация? - нет
    //    Геттер? - нет
    //    Private set? - нет (val)
    // ──────────────────────────────────────────────────────────
    val eventDate: String = "2026-10-15"

    // ──────────────────────────────────────────────────────────
    // 3. Место проведения
    //    Как назвать? - venueLocation
    //    Секретно? - нет
    //    Изменяется? - нет - val
    //    При инициализации? - да
    //    Остальное — нет
    // ──────────────────────────────────────────────────────────
    val venueLocation: String = "ул. Примерная, д1"

    // ──────────────────────────────────────────────────────────
    // 4. Подробный бюджет (расходы на оборудование, кейтеринг, прочее)
    //    Как назвать? - equipmentCost, cateringCost, otherOperationalCosts
    //    Секретно? - нет (но можно сделать private, если бюджет закрытый)
    //    Изменяется? - да - var (расходы корректируются по ходу)
    //    При инициализации? - да (стартовые сметы)
    //    Валидация? - ДА: значения не могут быть отрицательными
    //    - кастомный сеттер с require
    // ──────────────────────────────────────────────────────────
    var equipmentCost: Double = 120_000.0
        set(value) {
            require(value >= 0.0) { "Расход на оборудование не может быть отрицательным" }
            field = value
        }

    var cateringCost: Double = 80_000.0
        set(value) {
            require(value >= 0.0) { "Расход на кейтеринг не может быть отрицательным" }
            field = value
        }

    var otherOperationalCosts: Double = 300_000.0
        set(value) {
            require(value >= 0.0) { "Операционные расходы не могут быть отрицательными" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 10. Бюджет мероприятия (общий)
    //     Как назвать? - totalBudget
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - не нужно хранить — вычисляется из частей
    //     Геттер? - ДА: сумма трёх составляющих
    // ──────────────────────────────────────────────────────────
    val totalBudget: Double
        get() = equipmentCost + cateringCost + otherOperationalCosts

    // ──────────────────────────────────────────────────────────
    // 5. Количество участников
    //    Как назвать? - participantCount
    //    Секретно? - нет
    //    Изменяется? - да - var (регистрация продолжается)
    //    При инициализации? - да (0 или стартовое значение)
    //    Валидация? - ДА: не может быть отрицательным и не может
    //      превышать вместимость площадки
    // ──────────────────────────────────────────────────────────
    var participantCount: Int = 0
        set(value) {
            require(value >= 0) { "Количество участников не может быть отрицательным" }
            require(value <= maxVenueCapacity) { "Превышена вместимость площадки ($maxVenueCapacity)" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 6. Длительность хакатона (в часах)
    //    Как назвать? - hackathonDurationHours
    //    Секретно? - нет
    //    Изменяется? - нет - val
    //    При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val hackathonDurationHours: Int = 48

    // ──────────────────────────────────────────────────────────
    // 7. Контактная информация (публичная часть)
    //    Как назвать? - contactInfo
    //    Секретно? - нет (это для участников)
    //    Изменяется? - нет - val
    //    При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val contactInfo: String = "+7 (999) 123-45-67, organizer@example.com"

    // ──────────────────────────────────────────────────────────
    // 7. Условия соглашений с поставщиками (закрытая часть)
    //    Как назвать? - supplierAgreementTerms
    //    Секретно? - ДА - private (коммерческая тайна)
    //    Изменяется? - нет - val
    //    При инициализации? - да
    // ──────────────────────────────────────────────────────────
    private val supplierAgreementTerms: String = "Договоры хранятся в юр. отделе, доступ по запросу"

    // ──────────────────────────────────────────────────────────
    // 8. Текущее состояние хакатона (статус)
    //    Как назвать? - status
    //    Секретно? - нет
    //    Изменяется? - да - var
    //    При инициализации? - да
    // ──────────────────────────────────────────────────────────
    private var status: String = "PREPARATION"

    // ──────────────────────────────────────────────────────────
    // 9. Список спонсоров
    //    Как назвать? - sponsors
    //    Секретно? - нет
    //    Изменяется? - содержимое может пополняться до старта
    //    При инициализации? - да
    //    Валидация? - нет
    // ──────────────────────────────────────────────────────────
    val sponsors: String ="Sponsor A"

    // ──────────────────────────────────────────────────────────
    // 11. Текущий уровень доступа к интернету
    //     Как назвать? - internetAccessTier
    //     Секретно? - нет
    //     Изменяется? - да - var (мониторинг меняет)
    //     При инициализации? - да
    //     Private set? - ДА: управляется системой мониторинг
    // ──────────────────────────────────────────────────────────
    var internetAccessTier: String = "HIGH"

    // ──────────────────────────────────────────────────────────
    // 12. Информация о транспортировке, распределении ресурсов,
    //     координации между командами поддержки
    //     Как назвать? - logisticsPlan
    //     Секретно? - ДА - private (внутренняя координация)
    //     Изменяется? - да - var (план уточняется)
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    private var logisticsPlan: String = "Доставка за 1 день до старта, координация через внутренний чат"

    // ──────────────────────────────────────────────────────────
    // 13. Количество команд
    //     Как назвать? - teamCount
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да (0)
    //     Валидация? - ДА: не может быть отрицательным
    // ──────────────────────────────────────────────────────────
    var teamCount: Int = 0
        set(value) {
            require(value >= 0) { "Количество команд не может быть отрицательным" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 14. Перечень задач
    //     Как назвать? - tasks
    //     Секретно? - нет (публичный список)
    //     Изменяется? - ссылка — нет - val; содержимое фиксировано
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val tasks: List<String> = listOf(
        "Разработать MVP",
        "Интегрировать API",
        "Подготовить демо"
    )

    // ──────────────────────────────────────────────────────────
    // 15. План эвакуации
    //     Как назвать? - evacuationPlan
    //     Секретно? - нет (должен быть доступен всем)
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val evacuationPlan: String = "План эвакуации размещён на стенде у входа, дублируется в чате"

    // ──────────────────────────────────────────────────────────
    // 16. Список доступного оборудования (всё, что выделено)
    //     Как назвать? - availableEquipment
    //     Секретно? - нет
    //     Изменяется? - ссылка — нет - val; содержимое фиксировано
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val availableEquipment: List<String> = listOf(
        "Ноутбуки x20", "Проекторы x3", "Маркерные доски x5", "Wi-Fi роутеры x4"
    )

    // ──────────────────────────────────────────────────────────
    // 17. Список свободного оборудования (что можно взять прямо сейчас)
    //     Как назвать? - freeEquipment
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
     val _freeEquipment: MutableList<String> = mutableListOf("Проектор №3", "Доска №2")

    val freeEquipment: List<String>
        get() = _freeEquipment.toList()

    // ──────────────────────────────────────────────────────────
    // 18. График питания участников
    //     Как назвать? - cateringSchedule
    //     Секретно? - нет
    //     Изменяется? - нет (после публикации) - val... но значение
    //       неизвестно при старте - lateinit var
    //     При инициализации? - НЕТ: зависит от поставщика, который
    //       определяется за неделю до начала
    //     - lateinit var
    // ──────────────────────────────────────────────────────────
    lateinit var cateringSchedule: String

    // ──────────────────────────────────────────────────────────
    // 19. План мероприятий на случай сбоев
    //     Как назвать? - contingencyPlan
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val contingencyPlan: String = "Сбой питания - резервный генератор; сбой интернета - мобильные точки"

    // ──────────────────────────────────────────────────────────
    // 20. Список экспертов и жюри
    //     Как назвать? - expertsAndJury
    //     Секретно? - нет
    //     Изменяется? - ссылка — нет - val; содержимое может дополняться
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val expertsAndJury: MutableList<String> = mutableListOf("Иван Иванов", "Анна Петрова")

    // ──────────────────────────────────────────────────────────
    // 21. Политика конфиденциальности
    //     Как назвать? - privacyPolicyUrl
    //     Секретно? - нет (публичный документ)
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val privacyPolicyUrl: String = "https://docs.example.com/privacy"

    // ──────────────────────────────────────────────────────────
    // 22. Приватные отзывы (фидбэк) участников и зрителей
    //     Как назвать? - privateFeedback
    //     Секретно? - ДА - private
    //     Изменяется? - да
    //     При инициализации? - да (пустой список)
    //     Private set? - не нужно, всё поле private
    // ──────────────────────────────────────────────────────────
    private val privateFeedback: MutableList<String> = mutableListOf()

    // ──────────────────────────────────────────────────────────
    // 23. Текущая температура в помещении (°C)
    //     Как назвать? - currentRoomTemperature
    //     Секретно? - нет
    //     Изменяется? - да - var (датчик обновляет)
    //     При инициализации? - да
    //     Валидация? - ДА:
    //     Private set? - ДА: обновляется только изнутри (системой мониторинга)
    // ──────────────────────────────────────────────────────────
    var currentRoomTemperature: Double = 22.5
        private set(value) {
            require(value in 10.0..40.0) { "Температура $value выходит за разумные пределы" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 24. Данные мониторинга сети (задержка, пропускная способность,
    //     потери пакетов) — для отладки приложений командами
    //     Как назвать? - networkMonitoringData
    //     Секретно? - нет (команды используют для отладки)
    //     Изменяется? - да - var (постоянно обновляется)
    //     При инициализации? - да
    //     Private set? - ДА: обновляется системой мониторинга
    //     Геттер? - можно форматировать для удобства команд
    // ──────────────────────────────────────────────────────────
    var networkMonitoringData: String = "Задержка: 25 мс, пропуск: 1 Гбит/с, потери: 0%"
        private set

    // ──────────────────────────────────────────────────────────
    // 25. Регулятор скорости интернета (понижающий коэффициент)
    //     Как назвать? - internetSpeedFactor
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да
    //     Валидация? - ДА: диапазон 0.0..1.0
    // ──────────────────────────────────────────────────────────
    var internetSpeedFactor: Double = 1.0
        set(value) {
            require(value in 0.0..1.0) { "Коэффициент должен быть в диапазоне 0.0..1.0" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 26. Уровень освещения (в люксах)
    //     Как назвать? - lightingLevelLux
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да
    //     Валидация? - ДА: не может быть отрицательным
    // ──────────────────────────────────────────────────────────
    var lightingLevelLux: Int = 500
        set(value) {
            require(value >= 0) { "Уровень освещения не может быть отрицательным" }
            field = value
        }

    // ──────────────────────────────────────────────────────────
    // 27. Лог событий мероприятия
    //     Как назвать? - eventLog
    //     Секретно? - нет (доступен организаторам)
    //     Изменяется? - да (только добавление)
    //     Private set? - ДА: запись управляется изнутри
    //     - backing property: наружу — readonly List
    // ──────────────────────────────────────────────────────────
    private val _eventLog: MutableList<String> = mutableListOf()

    val eventLog: List<String>
        get() = _eventLog.toList()

    fun logEvent(entry: String) {
        _eventLog.add(entry)
    }

    // ──────────────────────────────────────────────────────────
    // 28. Возможность получения медицинской помощи на мероприятии
    //     Как назвать? - medicalAssistanceAvailable
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val medicalAssistanceAvailable: Boolean = true

    // ──────────────────────────────────────────────────────────
    // 29. Планы и процедуры для обеспечения безопасности
    //     (включая план эвакуации и протоколы ЧС)
    //     Как назвать? - safetyProcedures
    //     Секретно? - нет (участники должны знать)
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val safetyProcedures: String = "Охрана на входе, медпункт, план эвакуации, тревожная кнопка"

    // ──────────────────────────────────────────────────────────
    // 30. Регистрационный номер мероприятия
    //     Как назвать? - registrationNumber
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val registrationNumber: String = "REG-2026-12345"

    // ──────────────────────────────────────────────────────────
    // 31. Максимально допустимый уровень шума (дБ)
    //     Как назвать? - maxAllowedNoiseLevelDb
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val maxAllowedNoiseLevelDb: Int = 60

    // ──────────────────────────────────────────────────────────
    // 32. Индикатор превышения уровня шума
    //     Как назвать? - noiseLevelExceeded
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да (false)
    //     Private set? - ДА: устанавливается системой мониторинга,
    //       извне — только чтение
    // ──────────────────────────────────────────────────────────
    var noiseLevelExceeded: Boolean = false
        private set

    // ──────────────────────────────────────────────────────────
    // 33. План взаимодействия с прессой
    //     Как назвать? - mediaInteractionPlan
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val mediaInteractionPlan: String = "Брифинги в 12:00 и 18:00, пресс-релиз по итогам"

    // ──────────────────────────────────────────────────────────
    // 34. Детальная публичная информация о проектах каждой команды
    //     (компиляция кода, статистика автотестов)
    //     Как назвать? - teamProjectsDetails
    //     Секретно? - нет (публичные данные)
    //     Изменяется? - нет - val
    //     При инициализации? - ТЯЖЁЛАЯ: требует компиляции и прогона
    //       автотестов - by lazy
    //     Геттер? - не нужен
    // ──────────────────────────────────────────────────────────
    val teamProjectsDetails: Map<String, String> by lazy {
        // Здесь — тяжёлая операция: компиляция, прогон тестов, агрегация
        mapOf(
            "Team Alpha" to "Проект: чат-бот для волонтёров — сборка OK, тесты 12/12",
            "Team Beta" to "Проект: трекер задач с AI — сборка OK, тесты 8/10"
        )
    }

    // ──────────────────────────────────────────────────────────
    // 35. Статус получения всех необходимых разрешений
    //     Как назвать? - permitsState
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да (PENDING)
    //     Private set? - ДА: меняется изнутри при получении документов
    // ──────────────────────────────────────────────────────────
    var permitsState: String = "PENDING"

    // ──────────────────────────────────────────────────────────
    // 36. Индикатор доступности специализированного оборудования
    //     Как назвать? - specializedEquipmentAvailable
    //     Секретно? - нет
    //     Изменяется? - да - var
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    var specializedEquipmentAvailable: Boolean = true


    // ──────────────────────────────────────────────────────────
    // 37. Список партнёров мероприятия
    //     Как назвать? - partners
    //     Секретно? - нет
    //     Изменяется? - ссылка — нет - val; содержимое может дополняться
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val partners: MutableList<String> = mutableListOf("Partner X", "Partner Y")

    // ──────────────────────────────────────────────────────────
    // 38. Отчёт (фото, видео, отзывы) — доступен после завершения
    //     Как назвать? - finalReportUrl
    //     Секретно? - нет (публичный отчёт)
    //     Изменяется? - нет (после генерации) - val... но значение
    //       неизвестно при старте - lateinit var
    //     При инициализации? - НЕТ: генерируется после завершения
    //     - lateinit var
    // ──────────────────────────────────────────────────────────
    lateinit var finalReportUrl: String

    // ──────────────────────────────────────────────────────────
    // 39. Внутренние правила распределения призов
    //     Как назвать? - prizeDistributionRules
    //     Секретно? - ДА - private (внутренние правила)
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    private val prizeDistributionRules: String = "Призы распределяются по решению жюри"

    // ──────────────────────────────────────────────────────────
    // 40. Список разыгрываемых призов
    //     Как назвать? - prizes
    //     Секретно? - нет
    //     Изменяется? - ссылка — нет - val; содержимое фиксировано
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val prizes: List<String> = listOf("Сертификат на обучение", "Ноутбук", "Подписка на сервисы")

    // ──────────────────────────────────────────────────────────
    // 41. Контактная информация экстренных служб (для внутреннего пользования)
    //     Как назвать? - emergencyContacts
    //     Секретно? - ДА - private ("для внутреннего пользования")
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    private val emergencyContacts: String = "Скорая: 103, Пожарная: 101, Охрана: +7 (999) 000-00-01"

    // ──────────────────────────────────────────────────────────
    // 42. Особые условия для участников с ограниченными возможностями
    //     Как назвать? - accessibilityConditions
    //     Секретно? - нет (должны быть публично видны)
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val accessibilityConditions: String = "Доступ для колясок, сопровождение, материалы в электронном виде"

    // ──────────────────────────────────────────────────────────
    // 43. Общее настроение участников (определяется опросами)
    //     Как назвать? - overallMood
    //     Секретно? - нет
    //     Изменяется? - нет (снимок на момент вычисления) - val
    //     При инициализации? - ТЯЖЁЛАЯ: требует анализа опросов - by lazy

    // ──────────────────────────────────────────────────────────
    val overallMood: String by lazy {"POSITIVE"}

    // ──────────────────────────────────────────────────────────
    // 44. Подробный план хакатона (время и содержание каждого сегмента)
    //     Как назвать? - detailedSchedule
    //     Секретно? - нет
    //     Изменяется? - нет (после публикации) - val... но значение
    //       неизвестно при старте - lateinit var
    //     При инициализации? - НЕТ: "инициализируется непосредственно
    //       перед началом мероприятия, так как зависит от фактического
    //       состава команд и взятых в работу задач"
    //     - lateinit var (значение устанавливается в конкретный момент,
    //       а не при первом обращении — поэтому не lazy)
    // ──────────────────────────────────────────────────────────
    lateinit var detailedSchedule: String

    // ──────────────────────────────────────────────────────────
    // 45. Имя знаменитого специального гостя
    //     Как назвать? - specialGuestName
    //     Секретно? - нет (будет публично объявлено)
    //     Изменяется? - нет (после объявления) - val... но значение
    //       неизвестно при старте - lateinit var
    //     При инициализации? - НЕТ
    // ──────────────────────────────────────────────────────────
    lateinit var specialGuestName: String

    // ──────────────────────────────────────────────────────────
    // 46. Максимальное количество людей, которое может вместить площадка
    //     Как назвать? - maxVenueCapacity
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val maxVenueCapacity: Int = 200

    // ──────────────────────────────────────────────────────────
    // 47. Количество часов, отведённое каждой команде для работы над проектом
    //     Как назвать? - teamWorkHours
    //     Секретно? - нет
    //     Изменяется? - нет - val
    //     При инициализации? - да
    // ──────────────────────────────────────────────────────────
    val teamWorkHours: Int = 40
}


